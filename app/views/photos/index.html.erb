<p>Here are all the photos for <%=@user.full_name %>.</p>


<% if @photos.empty? %>
	<p>This user has no photos</p>
<% else %>
	<% @photos.each do |p| %>
		<%= image_tag(p.filename, :size => "100x60", :alt => "Edit Entry") %>
	<% end %>

<% end %>